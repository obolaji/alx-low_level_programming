#include "main.h"
#include <stdio.h>

/**
 * main - prints the name of the program
 * @a: int
 * @b: char
 * Return: 0
 */

int main(int a, __attribute__((unused)) char **b)
{
	printf("%d\n", a - 1);
	return(0);
}
